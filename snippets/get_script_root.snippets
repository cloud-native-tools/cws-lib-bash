script_root=
if [ -n "${BASH_VERSION}" ]; then
    script_root=$(readlink -f $(dirname "${BASH_SOURCE[0]}"))
fi
if [ -n "${ZSH_VERSION}" ]; then
    script_root=$(readlink -f $(dirname "${(%):-%N}"))
fi