# Concepts

What the library is and why it is designed this way.

## Documents

- [feature-domains.md](feature-domains.md) — full catalog of functional and non-functional feature domains

_Candidate topics: library loading model (`profile.d/` vs `scripts/`), function naming and return-code conventions, cross-platform strategy._
